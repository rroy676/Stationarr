server {
    listen 80;
    server_name stationarr.yourdomain.com;   # <-- change this

    # Redirect HTTP to HTTPS (uncomment after setting up SSL)
    # return 301 https://$host$request_uri;

    client_max_body_size 50M;

    location / {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# Uncomment and configure after obtaining an SSL certificate (e.g. via certbot)
# server {
#     listen 443 ssl http2;
#     server_name stationarr.yourdomain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/stationarr.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/stationarr.yourdomain.com/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#
#     client_max_body_size 50M;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
